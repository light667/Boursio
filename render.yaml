services:
  - type: web
    name: boursio-webapp
    env: static
    rootDir: WebApp
    buildCommand: npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
